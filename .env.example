# Bullet Cluster Modeling - Environment Configuration Template
# Copy this file to .env and customize for your environment

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=spectral_analysis
DB_USER=bullet_user
DB_PASSWORD=your_secure_password_here

# Database Admin (for initial setup only)
DB_ADMIN_USER=postgres
DB_ADMIN_PASSWORD=your_postgres_admin_password

# Spark Configuration
SPARK_MASTER=local[*]
SPARK_DRIVER_MEMORY=4g
SPARK_EXECUTOR_MEMORY=2g

# SDSS/LAMOST API Configuration
SDSS_BATCH_SIZE=1000
LAMOST_API_KEY=your_api_key_if_needed

# Optional: Remote Database Configuration
# Uncomment and modify if using remote database
# DB_HOST=your-db-server.com
# DB_PORT=5432
# DB_SSL_MODE=require

# Optional: Spark Cluster Configuration
# Uncomment if using remote Spark cluster
# SPARK_MASTER=spark://your-spark-master:7077
# SPARK_DRIVER_HOST=your-driver-host